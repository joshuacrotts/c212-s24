import java.util.List;
import java.util.Map;
import java.util.Set;

class MapExamples {

  /**
   * Computes the mode of a given list of integers, i.e., the most
   * frequently-occurring value.
   * @param ls - list of integers.
   * @return set containing mode, or modes if there are multiple.
   */
  static Set<Integer> mode(List<Integer> ls) {
    return null;
  }

  /**
   * Given a list of strings, returns an alphabetized map of first letters
   * to the set of alphabetized words that start with that letter.
   * @param ls - list of strings.
   * @return map of associations.
   */
  static Map<String, Set<String>> shareFirstChar(List<String> ls) {
    return null;
  }

  /**
   * Given a string, returns the first letter that is unique in the string.
   * That is, the first letter that is not repeated. Order matters!
   * @param s - given string.
   * @return a single-character string.
   */
  static String firstUniqueLetter(String s) {
    return null;
  }
}
